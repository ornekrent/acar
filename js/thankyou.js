// This file primarily contains the inline script within thankyou.html
// and doesn't require a separate .js file due to its limited scope.
// The script handles the "Back to Homepage" button functionality.

document.addEventListener('DOMContentLoaded', () => {
  // Optionally clear local storage here if the booking is considered complete
  // localStorage.removeItem('initialBooking');
  // localStorage.removeItem('currentBooking');
});
